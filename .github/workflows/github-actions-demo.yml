name: Jacek Cierkosz 76259 Github Workflow
on: [push]
jobs:
    Test-github-actions:
    runs on: ubuntu-latest
    steps:
        - name: Check out repository code
          uses: actions/checkout@v3
        - run: echo "Moj pierwszy CI/CD Workflow"
        - run: docker --version
        - run: python --version

jobs:
    Test-github-actions:
    runs on: ubuntu-latest
    steps:
        - name: Check out repository code
          uses: actions/checkout@v3
        - run: Pylint app.py
jobs:
    Test-github-actions:
    runs on: ubuntu-latest
    steps:         
        - name: Check out repository code
          uses: actions/checkout@v3
        - run: docker build --tag test-flask-app
